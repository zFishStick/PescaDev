---
import {Image} from 'astro:assets';
import gabrielImg from '../assets/img/personal_pic/gabriel.jpg';

import { ui } from '../i18n/ui';
const lang = (Astro.currentLocale || 'it') as keyof typeof ui;
const t = ui[lang] as typeof ui['it'];

import '../styles/global.css';
---

<main>
  <div class="container intro">
      <div class="row">
          <div class="col">
            <Image src={gabrielImg} alt="Gabriel" />
          </div>
          <div class="col">
              <h1>{t.home.title}</h1>
              <p>{t.home.about}</p>
          </div>
      </div>
  </div>
</main>

<style>

  main {
    width: fit-content;
    margin: auto;
    caret-color: transparent;
  }

  .container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: auto;
    padding: 2rem;
    margin: 2rem auto;
    margin-bottom: 0;
    text-align: center;
    font-family: var(--font-base);
    color: white;
    background: var(--card-gradient);
    border-radius: 16px;
    box-shadow: 0 10px 24px rgba(184, 85, 46, 0.25);
    max-width: 900px;

    img {
      border-radius: 10px;
      width: 200px;
      height: auto;
    }
  }

  p {
    margin: 0;
  }
  
  .container h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .container p {
    font-size: 1.5rem;
  }

  .row {
    display: flex;
    flex-direction: row;
    gap: 20px;
    margin: 0;
  }

  .col {
    display: flex;
    flex-direction: column;
    text-align: start;
  }

    @media (max-width: 480px) {
        .intro {
            padding: 1rem;
            margin: 1rem;
        }

        .container img {
            width: 125px;
        }

        .container h1 {
          font-size: 1.3rem;
          margin-top: 0;
          margin-bottom: 0.6rem;
        }
    
        .container p {
          font-size: 1.2rem;
          line-height: 1.5rem;
        }
    }


</style>